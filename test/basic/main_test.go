package basic

import (
	"testing"

	"github.com/magiconair/properties/assert"
)

func TestAddOne(t *testing.T) {
	// var (
	// 	input  = 1
	// 	output = 2
	// )

	// actual := AddOne(input)
	// if actual != output {
	// 	t.Errorf("AddOne(%d), output=%d, output=%d", input, output, actual)
	// }
	assert.Equal(t, nil, nil, "AddOne(2) should be equal 3")
}
